// Создаем функцию для установки временной шкалы
func setTimeScale(scale: Float) {
    if scale < 0.1 {
        // Защита от неправильных значений временной шкалы
        print("Ошибка: Временная шкала не может быть меньше 0.1")
        return
    }
    
    // Применяем новую временную шкалу к движку
    Engine.setTimeScale(scale)
    
    // Выводим сообщение об успешной установке временной шкалы
    print("Временная шкала установлена на \(scale)")
}

// Используем функцию для установки временной шкалы на 1.0 (стандартная скорость)
setTimeScale(scale: 1.0)
