#include <stdio.h>

int compileForWindows(const char* gameName, const char* sourceCode) {
    // Здесь добавим сложную логику для компиляции игры для Windows.
    printf("Компиляция игры '%s' для Windows завершена успешно.\n", gameName);
    return 0;
}
