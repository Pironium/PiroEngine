# gpu_optimization.pis

# Функция для оптимизации работы с видеокартой Nvidia
def optimize_for_nvidia():
    # Здесь идет сложный и длинный код оптимизации для Nvidia
    # ...

# Функция для оптимизации работы с видеокартой AMD
def optimize_for_amd():
    # Здесь идет сложный и длинный код оптимизации для AMD
    # ...

# Главная функция для оптимизации видеокарт
def optimize_gpu():
    # Вызываем оптимизацию для Nvidia
    optimize_for_nvidia()
    
    # Вызываем оптимизацию для AMD
    optimize_for_amd()

# Теперь мы вызываем оптимизацию видеокарт при запуске
optimize_gpu()
